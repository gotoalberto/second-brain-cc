# Generated template: integrations/first-run installs it into ~/.config/systemd/user when
# the user accepts it; guardian.py keeps it in step. __VAULT__ and __HOME__ are rendered.
[Unit]
Description=Second Brain: keep the vault machinery wired

[Service]
Type=oneshot
WorkingDirectory=__VAULT__
Environment=BRAIN_JOB_LABEL=second-brain-guardian
ExecStart=/bin/sh __VAULT__/_bin/pywrap.sh __VAULT__/_bin/guardian.py repair
Nice=5
