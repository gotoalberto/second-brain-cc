"""google.py's core: named Google accounts, each with its own OAuth client and refresh token in KeePass.

domain.py holds the rules, application.py the use cases on ports.py, adapters.py the machine
(kp.py, urllib, files, a loopback server, Gmail), headless.py the token path unattended callers use.
"""
