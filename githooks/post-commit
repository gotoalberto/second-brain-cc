#!/bin/sh
# Brain post-commit hook. Generated by _bin/brain_watch.py install from 90-Meta/events.json.
# Do not edit by hand: edit the registry and run brain_watch.py install again.

ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0
# A clone without Brain's engine has nothing to run: never block it.
[ -x "$ROOT/_bin/pywrap.sh" ] || exit 0
# event: post-commit-dirty
"$ROOT/_bin/pywrap.sh" "$ROOT/_bin/events_core/git_post_commit.py" "$@" || exit $?
exit 0
